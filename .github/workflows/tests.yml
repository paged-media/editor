name: tests

# Editor Playwright surface → state-matrix evidence (W0.5).
#
# Decision-B model: the engine wasm ships in the published
# `@paged-media/canvas-wasm` package, so `pnpm install` is the whole
# engine setup — no `core` checkout, no build-from-source bridge. The
# vite dev server (spawned by playwright.config's `webServer`) sets the
# COOP/COEP headers SharedArrayBuffer needs; that middleware runs in the
# server process and works headless, so the chromium runner gets a
# cross-origin-isolated context with no extra flags.
#
# Plugin link chain (W3): `apps/canvas/package.json` pulls four bundles
# in via pnpm `link:` — draw-bundle (../../../plugin-draw/…), plugin-api
# + plugin-sdk (../../../plugin-sdk/…), and web-bundle
# (../../../plugin-web/…). Those `../../..` paths are relative to
# `apps/canvas/`, so they resolve to SIBLINGS of the editor repo root.
# We mirror the local ~/paged layout: check the editor out under
# `editor/` and the three plugin repos under their bare names, so e.g.
# `editor/apps/canvas/../../../plugin-draw` == `$GITHUB_WORKSPACE/plugin-draw`.
# The plugin repos are private; `STATE_DISPATCH_TOKEN` carries read scope
# across the paged-media org (same token plugin-draw's vitest.yml uses
# for its sdk checkout), with a github.token fallback for same-org runs.
# Install order is the workspace rule (plugin-sdk → plugin-draw →
# plugin-web → editor): each plugin repo's own internal `link:`s point at
# plugin-sdk, and the editor's `link:`s need all three sibling dirs
# present + installed before its own `--frozen-lockfile` install resolves.
#
# Scope: the behaviour surface (panels + gesture + e2e op-sandwiches),
# NOT the Envato fidelity gate. That gate needs the 4.4 GB
# `corpus/envato` LFS packs, their InDesign reference PDFs, poppler's
# `pdftoppm`, and the `paged-diff` Rust binary built from a `core`
# checkout — none of which the package-boundary runner carries. It stays
# a local / core-CI concern; `PAGED_CI_LEAN=1` drops `fidelity.spec.ts`
# + `extensive-corpus.spec.ts` from collection (both read the envato
# manifest at import time). The lean surface (~412 tests) still needs the
# small license-clear `corpus/generated` fixtures + `corpus/fonts`, which
# the harness loads — those come from a sparse LFS checkout of the
# private `paged-media/corpus` repo (≈25 MB, not the 4.4 GB envato tier).
#
# Sharding: playwright.config pins workers:1 (snapshot determinism), so
# the ~20-25 min surface is split across 3 shards (~7-9 min each + setup)
# via `--shard`, each emitting a `blob` report. A final job merges the
# blobs into one Playwright JSON and hands it to the shared
# `paged-media/state/actions/publish-results` action, which converts to
# contract-v1, applies `ingest/test-map.yaml` (the editor specs carry no
# @feat: tags yet), gates feature ids against the registry, and
# repository_dispatches `results-published` at the state repo.

on:
  push:
    branches: [main]
  pull_request:
    paths:
      - "apps/canvas/**"
      - "packages/**"
      - "pnpm-lock.yaml"
      - "package.json"
      - ".github/workflows/tests.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: tests-${{ github.ref }}
  cancel-in-progress: true

env:
  SHARDS: 3

jobs:
  playwright:
    name: playwright (shard ${{ matrix.shard }}/3)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        shard: [1, 2, 3]
    steps:
      # The editor goes under `editor/` so the apps/canvas `link:` paths
      # (../../../<plugin>) land on siblings under $GITHUB_WORKSPACE.
      - name: checkout editor
        uses: actions/checkout@v4
        with:
          path: editor

      # The three plugin sibling repos the apps/canvas link: chain needs.
      # plugin-sdk first (the others link: at it), then plugin-draw +
      # plugin-web. Private repos → STATE_DISPATCH_TOKEN (read scope on
      # the org), github.token fallback for same-org runs.
      - name: checkout plugin-sdk (link target)
        uses: actions/checkout@v4
        with:
          repository: paged-media/plugin-sdk
          path: plugin-sdk
          token: ${{ secrets.STATE_DISPATCH_TOKEN || github.token }}

      - name: checkout plugin-draw (link target)
        uses: actions/checkout@v4
        with:
          repository: paged-media/plugin-draw
          path: plugin-draw
          token: ${{ secrets.STATE_DISPATCH_TOKEN || github.token }}

      - name: checkout plugin-web (link target)
        uses: actions/checkout@v4
        with:
          repository: paged-media/plugin-web
          path: plugin-web
          token: ${{ secrets.STATE_DISPATCH_TOKEN || github.token }}

      # License-clear fixtures the harness loads (corpus/generated/*.idml
      # + corpus/fonts). Sparse + LFS so we pull ~25 MB, not the 4.4 GB
      # envato tier. The editor's vite config + tests resolve the corpus
      # as a sibling of the repo root (`editor/corpus`), so check it out
      # there. `STATE_DISPATCH_TOKEN` already has read scope on the
      # private paged-media siblings (used by plugin-draw for the sdk
      # checkout); fall back to github.token for same-org runs.
      - name: checkout corpus fixtures (sparse + LFS)
        uses: actions/checkout@v4
        with:
          repository: paged-media/corpus
          path: editor/corpus
          token: ${{ secrets.STATE_DISPATCH_TOKEN || github.token }}
          lfs: false
          sparse-checkout: |
            generated
            samples
            fonts
          sparse-checkout-cone-mode: true

      - name: pull LFS for the lean fixture tiers
        working-directory: editor/corpus
        run: |
          git lfs install --local
          git lfs pull --include="generated/**,samples/**,fonts/**"

      # Pin pnpm: with the editor checked out under editor/ (not at the
      # workspace root), action-setup can't read `packageManager` from a
      # root package.json — match the editor's pnpm@9.0.0 explicitly. v9
      # reads every repo's lockfile-v9 natively.
      - uses: pnpm/action-setup@v4
        with:
          version: 9

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: |
            editor/pnpm-lock.yaml
            plugin-sdk/pnpm-lock.yaml
            plugin-draw/pnpm-lock.yaml
            plugin-web/pnpm-lock.yaml

      # Install the link chain bottom-up (workspace rule): plugin-sdk,
      # then plugin-draw + plugin-web (which link: at plugin-sdk), then
      # the editor (whose link:s need all three present + installed).
      # Each repo is its own workspace with a committed lockfile-v9, so
      # all stay --frozen-lockfile.
      - name: install plugin-sdk (link root)
        working-directory: plugin-sdk
        run: pnpm install --frozen-lockfile

      - name: install plugin-draw
        working-directory: plugin-draw
        run: pnpm install --frozen-lockfile

      - name: install plugin-web
        working-directory: plugin-web
        run: pnpm install --frozen-lockfile

      - name: install editor (engine wasm resolves from the npm package)
        working-directory: editor
        run: pnpm install --frozen-lockfile

      # The Playwright surface runs against the vite DEV server
      # (playwright.config `webServer: npx vite`), so no production build
      # is needed — but typecheck the canvas app so a wire/type break
      # fails fast here rather than as an opaque runtime error mid-suite.
      # (`pnpm -r build` is avoided: the `packages/*` have no build step
      # — they're consumed as workspace TS source — and `apps/devtools`
      # would pull in the unrelated introspect-wasm build.)
      - name: typecheck canvas app
        working-directory: editor
        run: pnpm --filter ./apps/canvas run typecheck

      - name: install chromium + system deps
        working-directory: editor
        run: pnpm --filter ./apps/canvas exec playwright install --with-deps chromium

      - name: playwright (lean surface, shard ${{ matrix.shard }})
        working-directory: editor/apps/canvas
        env:
          CI: "1"
          PAGED_CI_LEAN: "1"
        run: |
          npx playwright test \
            --shard=${{ matrix.shard }}/${{ env.SHARDS }} \
            --reporter=blob

      - name: upload blob report
        if: ${{ !cancelled() }}
        uses: actions/upload-artifact@v4
        with:
          name: blob-report-${{ matrix.shard }}
          path: editor/apps/canvas/blob-report
          retention-days: 7

  publish:
    name: merge reports + publish evidence
    needs: playwright
    if: ${{ !cancelled() }}
    runs-on: ubuntu-latest
    steps:
      # merge-reports only needs apps/canvas's @playwright/test (a real
      # npm dep), not the plugin link targets, so the editor checkout
      # alone suffices here. Keep it under `editor/` for path parity with
      # the playwright job. The apps/canvas `link:` deps resolve to
      # absent siblings as dangling symlinks (pnpm install does not fail
      # on that), and merge-reports never imports plugin code.
      - name: checkout editor
        uses: actions/checkout@v4
        with:
          path: editor

      - uses: pnpm/action-setup@v4
        with:
          version: 9

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: editor/pnpm-lock.yaml

      # Only the canvas app's @playwright/test is needed for merge-reports.
      - name: install (apps/canvas only)
        working-directory: editor
        run: pnpm install --frozen-lockfile --filter ./apps/canvas

      - name: download shard blobs
        uses: actions/download-artifact@v4
        with:
          path: all-blobs
          pattern: blob-report-*
          merge-multiple: true

      - name: merge shards → single Playwright JSON
        working-directory: editor
        env:
          PLAYWRIGHT_JSON_OUTPUT_NAME: ${{ github.workspace }}/playwright-results.json
        run: |
          pnpm --filter ./apps/canvas exec \
            playwright merge-reports --reporter=json ${{ github.workspace }}/all-blobs \
            > /dev/null
          test -s "${{ github.workspace }}/playwright-results.json"

      # Convert → contract-v1, apply ingest/test-map.yaml (no @feat: tags
      # in the editor yet), gate feature ids against the registry snapshot,
      # upload the normalized artifact, and dispatch results-published.
      # The editor surface registers as suite `playwright-e2e`
      # (ingest/suites.yaml; per-result stages from test-map override the
      # editor.canvas default so panel rows land on editor.panel etc.).
      - name: publish results to paged-media/state
        uses: paged-media/state/actions/publish-results@main
        with:
          source: editor
          suite: playwright-e2e
          format: playwright-json
          path: playwright-results.json
          map: "true"
          dispatch-token: ${{ secrets.STATE_DISPATCH_TOKEN }}
