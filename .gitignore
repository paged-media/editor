# Node / pnpm
node_modules/
dist/
*.tsbuildinfo

# Test + build artefacts
test-results/
playwright-report/
*.actual.png

# OS / editor / runtime
.DS_Store
.vscode/
.idea/
.claude/

# Local side-by-side-dev symlinks (→ ~/paged/corpus, ~/paged/core/target);
# the corpus assets live in the paged-media/corpus repo, never here.
/corpus
/target

# NOTE: wasm-bindgen build output (the gitignored .js / *_bg.wasm; only the
# .d.ts is tracked) is handled by the per-package .gitignore files under
# apps/*/ and packages/client/.
