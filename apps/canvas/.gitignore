node_modules/
dist/
# The wasm-bindgen output (.js loader + .wasm binary + bg.wasm.d.ts)
# lives in this directory during `bash build-wasm.sh` and is consumed
# by the worker; nothing in here is tracked. The tsify-generated
# protocol `.d.ts` moved to `packages/client/src/wasm/` so the
# `@paged-media/client` package is self-contained.
src/wasm/
*.local
tsconfig.tsbuildinfo
playwright-report/
test-results/
