# `tests/showcase/assets/` — what the showcase places, and under what licence

The showcase builds a real document, so it places real files. They live
here rather than in `~/paged/corpus` for one reason: the corpus is a
**private** repo, and a generator that only runs where someone has it
checked out is a generator that does not run. Everything in this folder
is redistributable, small, and carries its grant below.

| file | what | origin | licence |
|---|---|---|---|
| `showcase-cat.jpg` | 320×240 progressive JPEG photograph, 21 KB | `tests/images/jpg/progressive/cat.jpg` from [`image-rs/image`](https://github.com/image-rs/image), upstream commit `44ce9226c541dd2b11897b9ef07156a28871cdb7`; mirrored locally at `~/paged/corpus/raster/image-rs/` | **MIT OR Apache-2.0** — image-rs's own dual licence. See that tree's `PROVENANCE.md`: this file is in the 165-asset "everything else" tier, the one with a grant in the box, not in the vendored `png/apng/wpt/` (BSD-3-Clause) or `tga/testsuite/` (rights never established) subtrees. |
| `showcase-cat-cmyk.jpg` | CMYK JPEG, 320×240, with core's `default_cmyk.icc` embedded (235 KB) | derived from `showcase-cat.jpg` by `gen-cmyk-jpeg.py` (byte-stable; rerun to regenerate) | **MIT OR Apache-2.0** — same grant as its source; the ICC profile is core's own distributable default. |
| `showcase-mark.svg` | "Signal" — the vector mark page 6 imports | authored here for this repository | **AGPL-3.0-only OR PMEL**, the same dual licence as the rest of the editor. |

## Why a progressive JPEG in particular

Page 5 is the paged.image page, and its job is to prove the raster lane
end to end on a file the engine has to genuinely decode. A progressive
JPEG is the least convenient shape of that lane — a synthesized PNG
would prove the plumbing while dodging the codec — and it is the sample
`image-codecs::JpegSource` is itself tested against upstream.

## Coordinates in `showcase-mark.svg`

The paged.draw SVG importer takes SVG user units as PAGE POINTS and
lowers each shape through `insertPath` unscaled, so the file is authored
directly in US Letter page space (612×792, origin top-left) inside the
base fixture's 72 pt margins. It is not artwork that gets fitted; it is
drawn where it lands. The shape ORDER is part of the contract with
`pages/06-vector.ts` — see the comment at the top of the SVG.

## Why a CMYK JPEG with an embedded profile

The picture chapter's format gallery must exercise the engine's CMYK-JPEG
decode lane (DCT-scaled, embedded-ICC) — the one raster shape none of the
license-clear corpus images has. `gen-cmyk-jpeg.py` derives it from the
already-cleared cat photo, so the grant chain stays one line long.

## The Paged Annual battery (WP3)

The "Paged Annual" document is the all-plugins showcase, and its inputs
follow the same rule as everything above: authored fresh for this
repository or derived from an already-cleared source, generated by a
COMMITTED byte-stable script wherever generation makes sense (fixed zip
timestamps, no randomness — run any `gen-*.py` twice and the bytes
match), and granted below. Photography lives in `photos/` with its own
README.

| file | what | origin | licence |
|---|---|---|---|
| `annual-charts.xlsx` | the annual's workbook: a `Data` sheet of quarterly circulation series + a `Charts` sheet anchoring TEN DrawingML charts, one per engine `ChartKind` (column/bar/stacked-column/stacked-bar/line/area/pie/donut/scatter/radar) | byte-identical copy of `plugin-sheets/corpus/xlsx-corpus/annual-charts.xlsx`, generated by the committed `generate-annual.py` there; the importer contract is `sheet-conformance/tests/chart.rs::sheet_chart_xlsx_part_kind_battery` | **AGPL-3.0-only OR PMEL** — authored in the plugin-sheets repo under its own dual licence; no third-party content. |
| `annual-report.docx` | 3-4 page Word circulation report exercising every tier paged.doc lowers: docDefaults + 4 styles, direct formatting, 2-level bullet AND decimal lists, a gridSpan + vMerge table, one embedded (generated) PNG, both hyperlink forms, two footnotes, tab stops, keepNext | `gen-annual-report-docx.py` here (stdlib, byte-stable); the importer contract is plugin-doc `docx-conformance/tests/annual_report.rs` over a byte-identical committed copy | **AGPL-3.0-only OR PMEL** — prose and image self-authored for this repository. |
| `gen-annual-report-docx.py` | the generator above | authored here | **AGPL-3.0-only OR PMEL**. |
| `web/long-read.html` | the web chapter's CSS Regions article: primary `article-flow` + named `sidebar-flow` (the flow-picker case) + `flow-from` consumers; visuals stay inside web-render's documented render+bake set (solid fills, border-radius, solid strokes, multi-run text, one conic gradient, one outset shadow — deferred-count zero by design) | authored here | **AGPL-3.0-only OR PMEL**. |
| `web/fares-table.html` | the fragmenting `<table>`: a TWO-row `<thead>` (both rows repeat per frame) + 40 body rows, tall enough that it must split across linked frames | authored here | **AGPL-3.0-only OR PMEL**. |
| `web/nested-cards.html` | the nested-container split case: element-children-only cards in a column (recursion through `splittable_container_children`) with one atomic data-URI `<img>` that must move whole | authored here (the PNG data URI is generated, deterministic) | **AGPL-3.0-only OR PMEL**. |
| `annual-crest.svg` | the annual's cover crest: compound paths with evenodd holes + curves, drawn in the annual's 540x720 page space (the importer lowers user units as page points, unscaled) | authored here; parses through the real draw-geometry importer (4 shapes, 2 compound) | **AGPL-3.0-only OR PMEL**. |
| `annual-diagram.svg` | the frontispiece pipeline diagram: four `<g translate>` stations + arrowed rails, same page space, textless by the importer's scope | authored here; parses through the real draw-geometry importer (20 shapes, transforms flattened) | **AGPL-3.0-only OR PMEL**. |
| `annual-legacy.eps` | the legacy-format exhibit: a minimal hand-written EPSF-3.0 (~20 lines). The engine deliberately does NOT decode EPS — this file's job is to exist, be recognisably EPS, and render as the honest placeholder | authored here | **AGPL-3.0-only OR PMEL**. |
| `annual-orders.csv` | the data chapter's order book: 48 deterministic rows — 4 regions for grouping, ints/decimals/ISO dates, Code-128-able SKUs, VALID EAN-13 + UPC-A check digits (computed), per-order URLs for QR, some empty notes for ISBLANK/COALESCE | `gen-annual-orders.py` here (stdlib, no randomness) | **AGPL-3.0-only OR PMEL** — fictional, self-authored. |
| `gen-annual-orders.py` | the generator above | authored here | **AGPL-3.0-only OR PMEL**. |
| `annual-layers.psd` | 400x300 RGB-8 layered PSD: Backdrop (gradient, norm, 255) / Plate (mul, 200) / Signal (scrn, 160); mask-free by design — the layer-import tier refuses masked files by its documented envelope | byte-identical copy of `plugin-image/image-psd/tests/fixtures/annual-layers.psd`, emitted + byte-stability-proven by `image-psd/tests/annual_fixture.rs` there (both rendered tiers asserted) | **AGPL-3.0-only OR PMEL** — assembled by the crate's own spec-§10.4 builders; no third-party bytes. |
