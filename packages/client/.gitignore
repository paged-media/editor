# Generated by `bash apps/canvas/build-wasm.sh`. Only the tsify'd
# `.d.ts` is tracked (vendored so PR diffs show wire-format changes);
# the .js loader and .wasm binary are rebuilt locally.
src/wasm/*
!src/wasm/idml_canvas_wasm.d.ts
