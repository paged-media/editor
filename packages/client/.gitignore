# Decision-B package boundary: the engine wasm + its tsify'd `.d.ts`
# now ship in the published `@paged-media/canvas-wasm` package and are
# resolved from node_modules — there is no longer a vendored
# `src/wasm/` directory to ignore. (Pre-flip this file pinned the
# vendored `paged_canvas_wasm.d.ts` and ignored the rebuilt .js/.wasm;
# both are gone.)
